# JEI++ 1.0.2 之后的更新总结

本文记录 JEI++ 从 1.0.2 之后到当前代码的功能变化，包含已提交版本以及当前工作区中的后续实现。

## 版本与操作方式

- 版本号进入 1.0.3 系列，并继续同时维护 Minecraft 1.20.1 Forge 与 1.21.1 NeoForge 两个版本。
- 配方树书签的自动合成快捷键由 Shift+左键调整为 Ctrl+左键，避免与新版 JEI 及 AE2、RS 等网络终端对 Shift+左键的拦截产生冲突。
- `preferRecipeBookmarkOnOutput` 与 `hideRecipeBookmarkButton` 拆分为两个独立配置项，是否优先收藏配方与是否隐藏 JEI 原生按钮可以分别控制。

## 配方浏览与书签

- 增加多候选物品的分页目录。配方槽包含多个等价输入时，可以打开目录查看所有候选，并继续查看每个候选的配方、用途和标签信息。
- 标签查看不再被当作配方页处理。
- 完善配方页滚轮导航：顶部箭头和页码区域翻整页，配方名称行切换配方页，配方分类图标行切换分类。
- 书签配方查看不再限制为单个配方：所有匹配配方和用途都会保留，收藏配方所在分类和配方会被优先显示。
- 多输出配方按照鼠标所指的具体输出物品确定收藏目标，不再固定使用第一个输出。
- 只有同时满足 JEI 自身的书签优先排序和 JEI++ 的配方收藏优先配置时，收藏输出槽才会收藏配方；输入槽仍然收藏物品。
- 默认配方支持按输出物品保存、多输出配方的部分选择，并保存到 `recipe_defaults.json`。原木/去皮原木到木板以及铁粒到铁锭等不适合作为通用默认的配方被排除在自动默认选择之外，但仍可手动设为默认。

## 配方树与制作助手

- 完成类似 EMI 的配方树视图：节点、连线、配方分类、输入输出、折叠展开、缩放、拖动、自动适配完整视图和重新居中。
- 配方树支持选择具体根输出、替换节点配方、在树内选择固定候选材料、解除固定，以及合并等价输入槽。
- 未固定候选时，所有候选都会参与库存数量、背包高亮和 JEI 配方转移。候选优先使用背包或网络中已有的物品，并递归查找可制作的材料链，不再通过库存评分擅自回退到另一条配方。
- 默认配方、配方书签和物品书签都会参与配方树选择；书签中的中间产物也会被应用。
- 制作模式支持目标份数、批量数量、循环保护、深度/节点限制、总耗材、副产物和剩余材料统计。最终产物、总耗材与副产物居中对齐，最终产物、中间产物、基础材料和缺少/已有状态使用不同颜色区分。
- 配方树书签会按最终产物、中间产物、基础材料分行显示，已拥有的材料也会继续显示；数量实时读取背包和网络库存。
- 普通左键只转移当前步骤的直接材料，不进行递归，也不会因为找不到直接材料而强行打开配方；Ctrl+左键只递归处理缺少的步骤。
- 即时工作方块会在可行时自动取出中间产物，有加工时间的机器只放入材料；兼容的网络终端使用其客户端接口或通用容器转移路径。
- 配方节点右下角的 `+` 可直接执行 JEI 风格的当前配方转移，Shift+点击转移完整数量；执行后关闭配方树和其他 JEI 配方覆盖层，返回工作方块界面。
- JEI 作弊模式下，配方树书签对最终产物和中间产物不拦截 JEI 原生拿取一个/一组物品的操作。
- 总配方树按钮支持打开、返回和右键清除，只在 JEI 相关界面显示；按 E 或 Esc 也可返回。
- 当前制作中的配方树保存到 `recipe_tree_session.json`，可跨存档重新进入、游戏重启和 JEI 重载恢复；主动切换或清除配方树时才会删除保存状态。

## 流体配方

- 配方树和书签直接显示流体，不把流体统一替换成桶。
- 需要明确的桶类物品时保持桶输入；配方需要流体时，流体和可用容器都可以作为候选，并可通过 `+` 固定具体候选。
- 支持流体输入、流体输出、多输出流体选择、流体能力槽位以及从流体桶等容器转移。
- 1000 mB 按 1 B 换算；不足 1000 mB 显示 mB，达到 1000 mB 后按一位小数显示 B。数量显示使用实际库存量，不被需求上限截断。

## AE、RS、超越维度与集成终端

- 增加可选的客户端反射式存储网络集成，不要求这些模组成为硬依赖。
- AE2、Refined Storage（RS1/RS2）、超越维度和集成动力/集成终端的物品与流体库存都可以参与配方树计算、数量显示、候选选择和高亮。
- 网络库存中的匹配物品和流体会优先显示在对应终端列表前方，并使用共享快照、修订号和按需扫描减少重复探测；在 RS、超越维度或集成终端界面中不会重复扫描 AE2。
- 自动转移优先使用终端提供的客户端接口、官方数据包或通用容器点击协议；在背包空间不足且目标终端支持时，产物可以放回网络。
- 缺少可选网络模组、API 变化或终端类型不匹配时，集成会安全跳过，不影响 JEI++ 的其他功能。

## 创造物品分类栏

- 在 JEI 物品列表上方增加全部物品和创造模式分类栏。
- 分类栏提供左右翻页按钮、整页滚轮翻页和居中的页码覆盖层，不占用普通物品槽位。
- 分类、物品贴图、数量和页码的绘制层级经过调整，数字覆盖层会显示在物品贴图之上。

## 物品分组与折叠

- 增加 Reliable-EMI 风格的可展开/折叠物品分组，支持羊毛、木板、原木、矿石、工具、玻璃、门、台阶等内置分组。
- 同一注册名但 NBT/组件不同的物品可以折叠，适用于药水、附魔书等变体。
- 支持 Tag 分组、正则表达式分组和 JSON 自定义分组；分组具有优先级、翻译名/直接名称、排除项和启用开关。
- 每个默认分组有独立开关，可控制不同 Mod 命名空间是否混组。
- 检测到 JEI Tag Groups、JEI Groups 或 Collapsible Groups 等外部分组模组时，JEI++ 会自动停用自身物品变换，避免重复处理。
- 分组展开、重新折叠、数量覆盖层和页码覆盖层的绘制顺序经过修正。

## 性能、兼容性与稳定性

- 配方候选、布局、库存快照、网络快照和 fluid 访问器使用缓存，并使用游戏刻刷新和修订号避免每帧重复扫描。
- 递归候选搜索具有深度、访问数量和循环保护；只有配方树和默认配方相关流程才进行递归查找。
- 兼容不同 JEI 版本的配方布局、书签、物品列表和渲染路径，并同时维护 1.20.1 Forge 与 1.21.1 NeoForge 的实现。
- JEI 重载、存档切换、终端切换和可选模组缺失时会清理或恢复运行时缓存，避免因旧界面对象或网络 API 变化导致崩溃。

## 当前配置与文件

- 客户端配置：`config/jei_plus_plus-client.toml`
- 默认配方：`config/jei_plus_plus/recipe_defaults.json`
- 配方树会话：`config/jei_plus_plus/recipe_tree_session.json`
- JSON 分组：`config/jei_plus_plus/stack_groups/*.json`
- 常用配置包括配方树、自动合成、物品折叠、NBT/Tag/JSON 分组、命名空间混组、默认分组、输出配方收藏优先级以及 JEI 原生书签按钮显示。

